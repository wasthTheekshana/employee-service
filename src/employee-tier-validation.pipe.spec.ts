import { EmployeeTierValidationPipe } from './employee-tier-validation.pipe';

describe('EmployeeTierValidationPipe', () => {
  it('should be defined', () => {
    expect(new EmployeeTierValidationPipe()).toBeDefined();
  });
});
